<p id="notice"><%= notice %></p>

<p>
  <strong>Path name:</strong>
  <%= @ontology.path_name %>

</p>

<%= link_to 'Edit', edit_ontology_path(@ontology) %> |
<%= link_to 'Back', ontologies_path %>

<p>
  <strong>Parse bpmn diagram to find the most suitable usertasks role for its task:</strong>
</p>

<%= form_tag(sparql_path, method: :get) do %>
    <div class="field">
      <%= hidden_field_tag(search: 'task_role') %>
      <%= hidden_field_tag(search: 'task_role') %>
      <%= hidden_field_tag(id: @ontology.id) %>
      <%= label_tag(:bpmn_name, 'BPMN process name:') %>
      <%= text_field_tag(:bpmn_name) %>

    </div>

    <div class="actions">
      <%= submit_tag('Find') %>
    </div>
<% end %>
